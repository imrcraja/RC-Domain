# DNS foundation

RC Domain's first prototype records A, AAAA, CNAME, MX, TXT, and NS records
and labels their status `TEST/PRIVATE`. It does not claim that `.etc` is a
globally delegated DNS TLD or publish an authoritative nameserver. The
interface and API are the migration seam for a future authoritative DNS
service.
